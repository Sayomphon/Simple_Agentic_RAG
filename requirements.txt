# Direct dependencies, pinned to the versions this project was built and
# tested against (LangChain ecosystem APIs move fast — pinning keeps a
# fresh clone reproducible).
langgraph==1.2.9
langchain-openai==1.4.1
langchain-core==1.5.1
rank-bm25==0.2.2
numpy==2.4.6
python-dotenv==1.2.2
typing_extensions==4.16.0
streamlit==1.60.0
